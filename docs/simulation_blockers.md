# 仿真开发问题记录

| 日期 | 阶段 | 问题与证据 | 当前处理 | 是否需要实验室处理 |
| --- | --- | --- | --- | --- |
| 2026-08-13 | 模型/物理 | 本地 `scene_plus.xml` 含 SO-100 Plus 网格、关节与自碰撞模型，但不含实验桌、D435i 安装位姿、被抓物体的真实碰撞形状和摩擦参数。 | 复用模型做关节与自碰撞预检；桌面/相机/物体均配置为 `simulation_only` 的研究假设。 | 是；回实验室后比较真实 TCP、接触与相机外参。 |
| 2026-08-13 | 视觉模型 | 当前离线环境不使用千问 API。 | `SimulatedColorVLM` 仅从渲染 RGB 像素框定位；提示词与真实 API 接口保留，网络实验未执行。 | 否；有 API 凭据时可另行对照。 |
| 2026-08-13 | 图形界面 | 当前验收以 headless 运行。 | 输出结构化轨迹、RGB-D 帧和 benchmark JSON；不依赖 GUI。 | 否。 |

下一条实验室命令（只在完成真机安全检查后执行，而非现在）：先运行项目既有的只读相机/机械臂验收，不得把任何 `configs/simulation_*` 文件传给真机入口。
